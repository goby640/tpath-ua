import { useNavigate } from 'react-router-dom';
const MainPage = () => {
    const navigate = useNavigate();
    return (
        <p className="text-center">Main page text</p>
    );
};

export default MainPage;